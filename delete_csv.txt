import os
import glob

def delete_csv_file(directory):
    csv_files = glob.glob(os.path.join(directory, "*.csv"))
    
    if not csv_files:
        print("No CSV file found in the directory.")
        return False

    for file in csv_files:
        try:
            os.remove(file)
            print(f"Deleted: {file}")
        except Exception as e:
            print(f"Error deleting {file}: {e}")
            return False
            
    return True

# Usage
directory_path = r"C:/Users/ADMIN/Desktop/Automation/input"
delete_csv_file(directory_path)